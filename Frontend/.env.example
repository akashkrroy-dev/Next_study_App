# Base URL of the backend API.
# - Local dev: leave empty → defaults to http://localhost:5000/api (Vite proxy not needed)
#   OR set to http://localhost:5000/api
# - Deployed on Render (single service): leave EMPTY → uses relative /api so no
#   cross-origin/cross-site cookie issues.
VITE_API_URL=